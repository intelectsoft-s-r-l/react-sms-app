export interface IAccount {}
const initialState = {};
const account = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default account;
